<%- include("../partials/_header") %>

<div class="container mt-5 fluid">
  <div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12">
      <table class="table table-responsive">
        <thead>
          <tr>
            <td>ID</td>
            <td>Fecha programada</td>
            <td>Descripción</td>
            <td>Estatus</td>
            <td>Opciones</td>
          </tr>
        </thead>
        <tbody>
          <% if (citas) { %> <% for(var i = 0; i < citas.length; i++) {
          %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= citas[i].fecha %></td>
            <td><%= citas[i].descripcion %></td>
            <td><%= citas[i].estatus %></td>
            <td class="d-flex gap-2">
              <a href="/updateCita/<%= citas[i].id %>" class="btn btn-info">
                Edit
              </a>
              <a href="/deleteCita/<%= citas[i].id %>" class="btn btn-danger">
                Delete
              </a>
            </td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include("../partials/_footer") %>
